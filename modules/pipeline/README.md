# Pipeline

Covers two agenda sessions on Day 1:

| Time | Session | Owner |
|------|---------|-------|
| 14:00-14:30 | Intro to the Pipeline — RPI, planning, sub-agents, fast feedback, determinism | Malte? |
| 14:30-16:00 | Pod block 2 — Pipeline: wire your factory's pipeline | Facilitated groups |

Short talk introducing the pipeline needed as a foundation for the AI Factory, then the pod block where participants wire their own: planning, sub-agents, feedback loops, determinism.

## Materials

- Deck: talk slides plus the pod briefing in one deck. Start: `npm run new -- pipeline slidev` (or `ts`).
- `exercises/`: the pod tasks, one folder per exercise.
- Content source: the pipeline/process ingredients in `/Users/tereza/repos/hw/software-factory-slides` (plan well, small increments, fast feedback, determinism, hooks).
